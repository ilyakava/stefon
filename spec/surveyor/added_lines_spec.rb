require 'spec_helper'

describe Stefon::Surveyor::AddedLines do
  describe '#score_added_lines' do
    context 'with multiple authors' do
      it "doesn't suggest an excluded author"
    end

    context 'with a single author' do
      it "doesn't suggest an excluded author"
    end
  end
end